// Generated by dynwinrt-codegen — do not edit
import { DynWinRtType, DynWinRtMethodSig, DynWinRtValue, DynWinRtArray, DynWinRtStruct, DynWinRtDelegate, WinGuid } from '@microsoft/dynwinrt';
import { IID_TypedEventHandler_StoreQueueItem_Object, TypedEventHandler_StoreQueueItem_Object_PARAM_TYPES } from './TypedEventHandler_StoreQueueItem_Object.js';
import { IID_TypedEventHandler_StoreQueueItem_StoreQueueItemCompletedEventArgs, TypedEventHandler_StoreQueueItem_StoreQueueItemCompletedEventArgs_PARAM_TYPES } from './TypedEventHandler_StoreQueueItem_StoreQueueItemCompletedEventArgs.js';
import { StoreQueueItemCompletedEventArgs } from './StoreQueueItemCompletedEventArgs.js';
import { StoreQueueItemKind } from './StoreQueueItemKind.js';
import { StoreQueueItemStatus } from './StoreQueueItemStatus.js';

const IID_IStoreQueueItem = WinGuid.parse('56d5c32b-f830-4293-9188-cad2dcde7357');
const IID_IStoreQueueItem2 = WinGuid.parse('69491ca8-1ad4-447c-ad8c-a95035f64d82');

const _IStoreQueueItem = DynWinRtType.registerInterface(
    "IStoreQueueItem", IID_IStoreQueueItem)
    .addMethod("get_ProductId", new DynWinRtMethodSig().addOut(DynWinRtType.hstring()))
    .addMethod("get_PackageFamilyName", new DynWinRtMethodSig().addOut(DynWinRtType.hstring()))
    .addMethod("get_InstallKind", new DynWinRtMethodSig().addOut(DynWinRtType.enumType('Windows.Services.Store.StoreQueueItemKind', ['Install', 'Update', 'Repair'], [0, 1, 2])))
    .addMethod("GetCurrentStatus", new DynWinRtMethodSig().addOut(DynWinRtType.runtimeClass('Windows.Services.Store.StoreQueueItemStatus', WinGuid.parse('9bd6796f-9cc3-4ec3-b2ef-7be433b30174'))))
    .addMethod("add_Completed", new DynWinRtMethodSig().addIn(DynWinRtType.parameterized(WinGuid.parse('9de1c534-6ae1-11e0-84e1-18a905bcc53f'), [DynWinRtType.runtimeClass('Windows.Services.Store.StoreQueueItem', WinGuid.parse('56d5c32b-f830-4293-9188-cad2dcde7357')), DynWinRtType.runtimeClass('Windows.Services.Store.StoreQueueItemCompletedEventArgs', WinGuid.parse('1247df6c-b44a-439b-bb07-1d3003d005c2'))])).addOut(DynWinRtType.structType('Windows.Foundation.EventRegistrationToken', [DynWinRtType.i64()])))
    .addMethod("remove_Completed", new DynWinRtMethodSig().addIn(DynWinRtType.structType('Windows.Foundation.EventRegistrationToken', [DynWinRtType.i64()])))
    .addMethod("add_StatusChanged", new DynWinRtMethodSig().addIn(DynWinRtType.parameterized(WinGuid.parse('9de1c534-6ae1-11e0-84e1-18a905bcc53f'), [DynWinRtType.runtimeClass('Windows.Services.Store.StoreQueueItem', WinGuid.parse('56d5c32b-f830-4293-9188-cad2dcde7357')), DynWinRtType.object()])).addOut(DynWinRtType.structType('Windows.Foundation.EventRegistrationToken', [DynWinRtType.i64()])))
    .addMethod("remove_StatusChanged", new DynWinRtMethodSig().addIn(DynWinRtType.structType('Windows.Foundation.EventRegistrationToken', [DynWinRtType.i64()])));

const _IStoreQueueItem2 = DynWinRtType.registerInterface(
    "IStoreQueueItem2", IID_IStoreQueueItem2)
    .addMethod("CancelInstallAsync", new DynWinRtMethodSig().addOut(DynWinRtType.iAsyncAction()))
    .addMethod("PauseInstallAsync", new DynWinRtMethodSig().addOut(DynWinRtType.iAsyncAction()))
    .addMethod("ResumeInstallAsync", new DynWinRtMethodSig().addOut(DynWinRtType.iAsyncAction()));

export function unpackEventRegistrationToken(v) {
    const s = v.asStruct();
    return { value: s.getI64(0) };
}
const _unpackEventRegistrationToken = unpackEventRegistrationToken;
export const EventRegistrationToken_Type = DynWinRtType.structType('Windows.Foundation.EventRegistrationToken', [DynWinRtType.i64()]);
const _EventRegistrationToken_Type = EventRegistrationToken_Type;
export function packEventRegistrationToken(v) {
    const s = DynWinRtStruct.create(EventRegistrationToken_Type);
    s.setI64(0, v.value);
    return s;
}
const _packEventRegistrationToken = packEventRegistrationToken;

export class StoreQueueItem {
    _obj;

    constructor(obj) {
        this._obj = obj.cast(IID_IStoreQueueItem);
    }
    get productId() {
        return _IStoreQueueItem.method(6).invoke(this._obj, []).toString();
    }
    get packageFamilyName() {
        return _IStoreQueueItem.method(7).invoke(this._obj, []).toString();
    }
    get installKind() {
        return _IStoreQueueItem.method(8).invoke(this._obj, []).toNumber();
    }
    getCurrentStatus() {
        return new StoreQueueItemStatus(_IStoreQueueItem.method(9).invoke(this._obj, []));
    }
    onCompleted(callback) {
        const wrapped = (__a0__, __a1__) => callback(new StoreQueueItem(__a0__), new StoreQueueItemCompletedEventArgs(__a1__));
        const handler = DynWinRtDelegate.create(IID_TypedEventHandler_StoreQueueItem_StoreQueueItemCompletedEventArgs, TypedEventHandler_StoreQueueItem_StoreQueueItemCompletedEventArgs_PARAM_TYPES, wrapped);
        const __token = _IStoreQueueItem.method(10).invoke(this._obj, [handler.toValue()]);
        return () => { _IStoreQueueItem.method(11).invoke(this._obj, [__token]); };
    }
    onceCompleted(callback) {
        const unsub = this.onCompleted((...args) => { unsub(); callback(...args); });
        return unsub;
    }
    offCompleted(token) {
        _IStoreQueueItem.method(11).invoke(this._obj, [token]);
    }
    onStatusChanged(callback) {
        const wrapped = (__a0__, __a1__) => callback(new StoreQueueItem(__a0__), __a1__);
        const handler = DynWinRtDelegate.create(IID_TypedEventHandler_StoreQueueItem_Object, TypedEventHandler_StoreQueueItem_Object_PARAM_TYPES, wrapped);
        const __token = _IStoreQueueItem.method(12).invoke(this._obj, [handler.toValue()]);
        return () => { _IStoreQueueItem.method(13).invoke(this._obj, [__token]); };
    }
    onceStatusChanged(callback) {
        const unsub = this.onStatusChanged((...args) => { unsub(); callback(...args); });
        return unsub;
    }
    offStatusChanged(token) {
        _IStoreQueueItem.method(13).invoke(this._obj, [token]);
    }
    as(InterfaceClass) {
        return InterfaceClass.from(this._obj);
    }
    async cancelInstallAsync(signal) {
        if (signal?.aborted) throw signal.reason;
        const _op = _IStoreQueueItem2.method(6).invoke(this._obj.cast(IID_IStoreQueueItem2), []);
        const _onAbort = signal ? () => { try { _op.cancel(); } catch (_ce) { /* cancel after completion is a no-op per WinRT spec */ } } : undefined;
        if (_onAbort) { signal.addEventListener('abort', _onAbort, { once: true }); if (signal.aborted) _onAbort(); }
        try {
            await _op.toPromise();
            return;
        } catch (_e) {
            if (signal?.aborted) throw signal.reason;
            throw _e;
        } finally {
            if (_onAbort) signal.removeEventListener('abort', _onAbort);
        }
    }
    async pauseInstallAsync(signal) {
        if (signal?.aborted) throw signal.reason;
        const _op = _IStoreQueueItem2.method(7).invoke(this._obj.cast(IID_IStoreQueueItem2), []);
        const _onAbort = signal ? () => { try { _op.cancel(); } catch (_ce) { /* cancel after completion is a no-op per WinRT spec */ } } : undefined;
        if (_onAbort) { signal.addEventListener('abort', _onAbort, { once: true }); if (signal.aborted) _onAbort(); }
        try {
            await _op.toPromise();
            return;
        } catch (_e) {
            if (signal?.aborted) throw signal.reason;
            throw _e;
        } finally {
            if (_onAbort) signal.removeEventListener('abort', _onAbort);
        }
    }
    async resumeInstallAsync(signal) {
        if (signal?.aborted) throw signal.reason;
        const _op = _IStoreQueueItem2.method(8).invoke(this._obj.cast(IID_IStoreQueueItem2), []);
        const _onAbort = signal ? () => { try { _op.cancel(); } catch (_ce) { /* cancel after completion is a no-op per WinRT spec */ } } : undefined;
        if (_onAbort) { signal.addEventListener('abort', _onAbort, { once: true }); if (signal.aborted) _onAbort(); }
        try {
            await _op.toPromise();
            return;
        } catch (_e) {
            if (signal?.aborted) throw signal.reason;
            throw _e;
        } finally {
            if (_onAbort) signal.removeEventListener('abort', _onAbort);
        }
    }
}

export class IStoreQueueItem2 {
    _obj;

    constructor(obj) {
        this._obj = obj;
    }

    static from(obj) {
        return new IStoreQueueItem2(obj.cast(IID_IStoreQueueItem2));
    }

    async cancelInstallAsync(signal) {
        if (signal?.aborted) throw signal.reason;
        const _op = _IStoreQueueItem2.method(6).invoke(this._obj, []);
        const _onAbort = signal ? () => { try { _op.cancel(); } catch (_ce) { /* cancel after completion is a no-op per WinRT spec */ } } : undefined;
        if (_onAbort) { signal.addEventListener('abort', _onAbort, { once: true }); if (signal.aborted) _onAbort(); }
        try {
            await _op.toPromise();
            return;
        } catch (_e) {
            if (signal?.aborted) throw signal.reason;
            throw _e;
        } finally {
            if (_onAbort) signal.removeEventListener('abort', _onAbort);
        }
    }

    async pauseInstallAsync(signal) {
        if (signal?.aborted) throw signal.reason;
        const _op = _IStoreQueueItem2.method(7).invoke(this._obj, []);
        const _onAbort = signal ? () => { try { _op.cancel(); } catch (_ce) { /* cancel after completion is a no-op per WinRT spec */ } } : undefined;
        if (_onAbort) { signal.addEventListener('abort', _onAbort, { once: true }); if (signal.aborted) _onAbort(); }
        try {
            await _op.toPromise();
            return;
        } catch (_e) {
            if (signal?.aborted) throw signal.reason;
            throw _e;
        } finally {
            if (_onAbort) signal.removeEventListener('abort', _onAbort);
        }
    }

    async resumeInstallAsync(signal) {
        if (signal?.aborted) throw signal.reason;
        const _op = _IStoreQueueItem2.method(8).invoke(this._obj, []);
        const _onAbort = signal ? () => { try { _op.cancel(); } catch (_ce) { /* cancel after completion is a no-op per WinRT spec */ } } : undefined;
        if (_onAbort) { signal.addEventListener('abort', _onAbort, { once: true }); if (signal.aborted) _onAbort(); }
        try {
            await _op.toPromise();
            return;
        } catch (_e) {
            if (signal?.aborted) throw signal.reason;
            throw _e;
        } finally {
            if (_onAbort) signal.removeEventListener('abort', _onAbort);
        }
    }
}
