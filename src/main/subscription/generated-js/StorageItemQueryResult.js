// Generated by dynwinrt-codegen — do not edit
import { DynWinRtType, DynWinRtMethodSig, DynWinRtValue, DynWinRtArray, DynWinRtStruct, DynWinRtDelegate, WinGuid } from '@microsoft/dynwinrt';
import { IVectorView_IStorageItem } from './IVectorView_IStorageItem.js';
import { IID_TypedEventHandler_IStorageQueryResultBase_Object, TypedEventHandler_IStorageQueryResultBase_Object_PARAM_TYPES } from './TypedEventHandler_IStorageQueryResultBase_Object.js';
import { IID_IStorageItem, IStorageItem } from './IStorageItem.js';
import { StorageFolder } from './StorageFolder.js';
import { IID_IStorageQueryResultBase, IStorageQueryResultBase } from './IStorageQueryResultBase.js';
import { QueryOptions } from './QueryOptions.js';

const IID_IStorageItemQueryResult = WinGuid.parse('e8948079-9d58-47b8-b2b2-41b07f4795f9');

const _IStorageItemQueryResult = DynWinRtType.registerInterface(
    "IStorageItemQueryResult", IID_IStorageItemQueryResult)
    .addMethod("GetItemsAsync", new DynWinRtMethodSig().addIn(DynWinRtType.u32()).addIn(DynWinRtType.u32()).addOut(DynWinRtType.iAsyncOperation(DynWinRtType.parameterized(WinGuid.parse('bbe1fa4c-b0e3-4583-baef-1f1b2e483e56'), [DynWinRtType.interface(WinGuid.parse('4207a996-ca2f-42f7-bde8-8b10457a7f30'))]))))
    .addMethod("GetItemsAsyncDefaultStartAndCount", new DynWinRtMethodSig().addOut(DynWinRtType.iAsyncOperation(DynWinRtType.parameterized(WinGuid.parse('bbe1fa4c-b0e3-4583-baef-1f1b2e483e56'), [DynWinRtType.interface(WinGuid.parse('4207a996-ca2f-42f7-bde8-8b10457a7f30'))]))));

export function unpackEventRegistrationToken(v) {
    const s = v.asStruct();
    return { value: s.getI64(0) };
}
const _unpackEventRegistrationToken = unpackEventRegistrationToken;
export const EventRegistrationToken_Type = DynWinRtType.structType('Windows.Foundation.EventRegistrationToken', [DynWinRtType.i64()]);
const _EventRegistrationToken_Type = EventRegistrationToken_Type;
export function packEventRegistrationToken(v) {
    const s = DynWinRtStruct.create(EventRegistrationToken_Type);
    s.setI64(0, v.value);
    return s;
}
const _packEventRegistrationToken = packEventRegistrationToken;

export class StorageItemQueryResult {
    _obj;

    constructor(obj) {
        this._obj = obj.cast(IID_IStorageItemQueryResult);
    }
    async _getItemsAsync_1(signal) {
        if (signal?.aborted) throw signal.reason;
        const _op = _IStorageItemQueryResult.method(7).invoke(this._obj, []);
        const _onAbort = signal ? () => { try { _op.cancel(); } catch (_ce) { /* cancel after completion is a no-op per WinRT spec */ } } : undefined;
        if (_onAbort) { signal.addEventListener('abort', _onAbort, { once: true }); if (signal.aborted) _onAbort(); }
        try {
            const _v = await _op.toPromise();
            return new IVectorView_IStorageItem(_v);
        } catch (_e) {
            if (signal?.aborted) throw signal.reason;
            throw _e;
        } finally {
            if (_onAbort) signal.removeEventListener('abort', _onAbort);
        }
    }
    async _getItemsAsync_2(startIndex, maxNumberOfItems, signal) {
        if (signal?.aborted) throw signal.reason;
        const _op = _IStorageItemQueryResult.method(6).invoke(this._obj, [DynWinRtValue.u32(startIndex), DynWinRtValue.u32(maxNumberOfItems)]);
        const _onAbort = signal ? () => { try { _op.cancel(); } catch (_ce) { /* cancel after completion is a no-op per WinRT spec */ } } : undefined;
        if (_onAbort) { signal.addEventListener('abort', _onAbort, { once: true }); if (signal.aborted) _onAbort(); }
        try {
            const _v = await _op.toPromise();
            return new IVectorView_IStorageItem(_v);
        } catch (_e) {
            if (signal?.aborted) throw signal.reason;
            throw _e;
        } finally {
            if (_onAbort) signal.removeEventListener('abort', _onAbort);
        }
    }
    getItemsAsync(...args) {
        if (args.length >= 2 && args[0] !== undefined && !(args[0] instanceof AbortSignal)) {
            return this._getItemsAsync_2(args[0], args[1], args[2]);
        }
        return this._getItemsAsync_1(args[0]);
    }
    as(InterfaceClass) {
        return InterfaceClass.from(this._obj);
    }
}
