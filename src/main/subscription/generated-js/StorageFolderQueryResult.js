// Generated by dynwinrt-codegen — do not edit
import { DynWinRtType, DynWinRtMethodSig, DynWinRtValue, DynWinRtArray, DynWinRtStruct, DynWinRtDelegate, WinGuid } from '@microsoft/dynwinrt';
import { IVectorView_StorageFolder } from './IVectorView_StorageFolder.js';
import { IID_TypedEventHandler_IStorageQueryResultBase_Object, TypedEventHandler_IStorageQueryResultBase_Object_PARAM_TYPES } from './TypedEventHandler_IStorageQueryResultBase_Object.js';
import { StorageFolder } from './StorageFolder.js';
import { IID_IStorageQueryResultBase, IStorageQueryResultBase } from './IStorageQueryResultBase.js';
import { QueryOptions } from './QueryOptions.js';

const IID_IStorageFolderQueryResult = WinGuid.parse('6654c911-7d66-46fa-aecf-e4a4baa93ab8');

const _IStorageFolderQueryResult = DynWinRtType.registerInterface(
    "IStorageFolderQueryResult", IID_IStorageFolderQueryResult)
    .addMethod("GetFoldersAsync", new DynWinRtMethodSig().addIn(DynWinRtType.u32()).addIn(DynWinRtType.u32()).addOut(DynWinRtType.iAsyncOperation(DynWinRtType.parameterized(WinGuid.parse('bbe1fa4c-b0e3-4583-baef-1f1b2e483e56'), [DynWinRtType.runtimeClass('Windows.Storage.StorageFolder', WinGuid.parse('72d1cb78-b3ef-4f75-a80b-6fd9dae2944b'))]))))
    .addMethod("GetFoldersAsyncDefaultStartAndCount", new DynWinRtMethodSig().addOut(DynWinRtType.iAsyncOperation(DynWinRtType.parameterized(WinGuid.parse('bbe1fa4c-b0e3-4583-baef-1f1b2e483e56'), [DynWinRtType.runtimeClass('Windows.Storage.StorageFolder', WinGuid.parse('72d1cb78-b3ef-4f75-a80b-6fd9dae2944b'))]))));

export function unpackEventRegistrationToken(v) {
    const s = v.asStruct();
    return { value: s.getI64(0) };
}
const _unpackEventRegistrationToken = unpackEventRegistrationToken;
export const EventRegistrationToken_Type = DynWinRtType.structType('Windows.Foundation.EventRegistrationToken', [DynWinRtType.i64()]);
const _EventRegistrationToken_Type = EventRegistrationToken_Type;
export function packEventRegistrationToken(v) {
    const s = DynWinRtStruct.create(EventRegistrationToken_Type);
    s.setI64(0, v.value);
    return s;
}
const _packEventRegistrationToken = packEventRegistrationToken;

export class StorageFolderQueryResult {
    _obj;

    constructor(obj) {
        this._obj = obj.cast(IID_IStorageFolderQueryResult);
    }
    async _getFoldersAsync_1(signal) {
        if (signal?.aborted) throw signal.reason;
        const _op = _IStorageFolderQueryResult.method(7).invoke(this._obj, []);
        const _onAbort = signal ? () => { try { _op.cancel(); } catch (_ce) { /* cancel after completion is a no-op per WinRT spec */ } } : undefined;
        if (_onAbort) { signal.addEventListener('abort', _onAbort, { once: true }); if (signal.aborted) _onAbort(); }
        try {
            const _v = await _op.toPromise();
            return new IVectorView_StorageFolder(_v);
        } catch (_e) {
            if (signal?.aborted) throw signal.reason;
            throw _e;
        } finally {
            if (_onAbort) signal.removeEventListener('abort', _onAbort);
        }
    }
    async _getFoldersAsync_2(startIndex, maxNumberOfItems, signal) {
        if (signal?.aborted) throw signal.reason;
        const _op = _IStorageFolderQueryResult.method(6).invoke(this._obj, [DynWinRtValue.u32(startIndex), DynWinRtValue.u32(maxNumberOfItems)]);
        const _onAbort = signal ? () => { try { _op.cancel(); } catch (_ce) { /* cancel after completion is a no-op per WinRT spec */ } } : undefined;
        if (_onAbort) { signal.addEventListener('abort', _onAbort, { once: true }); if (signal.aborted) _onAbort(); }
        try {
            const _v = await _op.toPromise();
            return new IVectorView_StorageFolder(_v);
        } catch (_e) {
            if (signal?.aborted) throw signal.reason;
            throw _e;
        } finally {
            if (_onAbort) signal.removeEventListener('abort', _onAbort);
        }
    }
    getFoldersAsync(...args) {
        if (args.length >= 2 && args[0] !== undefined && !(args[0] instanceof AbortSignal)) {
            return this._getFoldersAsync_2(args[0], args[1], args[2]);
        }
        return this._getFoldersAsync_1(args[0]);
    }
    as(InterfaceClass) {
        return InterfaceClass.from(this._obj);
    }
}
